enum listFilterKeys {
    Company = 'Company',
    Cities = 'Cities',
    States = 'States'
  }
  
  enum listFilterKeysSalary {
    min = 'min',
    max = 'max'
  }
  
  enum checkboxesFilters {
    modality = 'modality',
    status = 'status'
  }

export {listFilterKeys, listFilterKeysSalary, checkboxesFilters}